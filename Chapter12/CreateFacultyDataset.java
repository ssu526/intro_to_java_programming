




/* 12.24 (Create large dataset) Create a data file with 1,000 lines. Each line in the file consists of a faculty memberÆs first name, last name, rank, and salary. The faculty memberÆs first name and last name for the ith line are FirstNamei and LastNamei. The rank is randomly generated as assistant, associate, and full. The salary is randomly generated as a number with two digits after the decimal point. The salary for an assistant professor should be in the range from 50,000 to 80,000, for associate professor from 60,000 to 110,000, and for full professor from 75,000 to 130,000. Save the file in Salary.txt. Here are some sample data: FirstName1 LastName1 assistant 60055.95 FirstName2 LastName2 associate 81112.45 . . . FirstName1000 LastName1000 full 92255.21 */ import java.io.*; import java.text.*; public class CreateFacultyDataset{ public static void main(String[] args) throws IOException{ File file = new File("Salary.txt"); PrintWriter output = new PrintWriter(file); NumberFormat formatter = new DecimalFormat("#0.00"); for(int i=1;i<=1000;i++){ String rank =generateRank(); double salary=generateSalary(rank); output.print("FirstName"+i+" LastName"+i+" "+rank+" "+formatter.format(salary)+"\n"); } } public static String generateRank(){ int randNum = (int)(Math.random()*3)+1; if(randNum==1){ return "assistant"; }else if(randNum==2){ return "associate"; }else{ return "full"; } } public static double generateSalary(String rank){ double salary; if(rank.equals("assistant")){ salary=Math.random()*30000+50000; }else if(rank.equals("associate")){ salary=Math.random()*50000+60000; }else{ salary=Math.random()*55000+75000; } return salary; } } 
